<%# if item.product = Product.find_by(id: product_id) %>
  <%# item_total = product.price * quantity %>
  <% @cart_total += item.total_price %>

  <tr>
    <td>
      <%= image_tag item.product.image.tiny %>
    </td>
    <td>
      <h4><%= item.product.name %></h4>
    </td>
    <td>
      <%= item.item_price %>
    </td>
    <td>
      <%= item.quantity %>
    </td>
    <td>
      <%= item.total_price %>
    </td>
  </tr>

<%# end %>
